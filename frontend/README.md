# Rail Mind Frontend

This repository contains the frontend application for Rail Mind — a Progressive Web App (PWA) built with Vite + TypeScript + React using Tailwind CSS and shadcn-ui.

## PWA Features ✨

This app is a **Progressive Web App** with:

- 📱 **Installable** on mobile and desktop devices
- 🚀 **Offline capable** with service worker caching
- ⚡ **Fast loading** with app shell caching
- 📊 **Lighthouse PWA score >= 90**

### PWA Testing & Installation

1. **Build and preview**: `npm run build && npm run preview`
2. **Open Chrome** and go to `http://localhost:4173/`
3. **Test install**: Look for install button in address bar or Chrome menu
4. **Test offline**: Disconnect internet, refresh - app should still load
5. **Lighthouse audit**: Chrome DevTools > Lighthouse > PWA category

## Local development

Prerequisites:

- Node.js (LTS recommended)
- npm or yarn

Quick start:

```bash
cd rail-mind/frontend
npm install
npm run dev
```

Common commands:

- `npm run dev` — start dev server (hot reload)
- `npm run build` — build production assets
- `npm run preview` — locally preview the production build
- `npm run test` — run tests
- `npm run lint` — check code quality

## Environment & configuration

Place environment variables in a `.env` file in the `frontend/` folder if needed (example: `VITE_API_URL=https://api.example.com`). Do not commit secrets.

## Deployment

This is a standard static frontend that can be deployed to Vercel, Netlify, GitHub Pages, or any static host. Use the build step (`npm run build`) and publish the `dist/` output.

## Tech stack

- **Vite** - Fast build tool and dev server
- **TypeScript** - Type-safe JavaScript
- **React** - UI library
- **Tailwind CSS** - Utility-first CSS framework
- **shadcn-ui** - Component library
- **PWA** - Progressive Web App capabilities via vite-plugin-pwa

## PWA Configuration

The PWA setup includes:

- **Web App Manifest** (`public/manifest.json`)
- **Service Worker** (auto-generated by Vite PWA plugin)
- **Offline caching** for app shell and API calls
- **Auto-update** functionality
- **App icons** in `public/icons/` (192x192 and 512x512)

### PWA Files Structure

```
frontend/
├── public/
│   ├── manifest.json          # Web app manifest
│   └── icons/                 # PWA icons
│       ├── icon-192.png
│       └── icon-512.png
├── vite.config.ts            # PWA plugin config
└── src/main.tsx              # Service worker registration
```

## Environment & configuration

Place environment variables in a `.env` file in the `frontend/` folder if needed (example: `VITE_API_URL=https://api.example.com`). Do not commit secrets.

## Deployment

This PWA can be deployed to any static host with HTTPS support:

- **Vercel**, **Netlify**, **GitHub Pages**
- Use the build step (`npm run build`) and publish the `dist/` output
- **HTTPS is required** for PWA features to work properly

## Contributing

- Edit files locally and open a PR
- Follow repository linting and formatting rules if present
- Test PWA functionality after changes affecting service worker or manifest
